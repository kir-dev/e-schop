<p>
<strong>Név:</strong>
<%= @good.name %>
</p>

<p>
<strong>Ár:</strong>
<%= @good.price %>
</p>

<p>
<strong>Eladó:</strong>
<%= @user.username %>
</p>

<p>
<strong>Leírás:</strong>
<%= @good.description %>
</p>
<% name = User.find_by_id(session[:user_id]).name %>
<% uzenet = name + " megvette fent jelzett nevű terméked, 
            Dátum: " + Time.new.strftime("%Y-%m-%d, %H:%M:%S").to_s%>
<p><%=  link_to 'Kosárba', {:action => 'to_cart', :controller => 'goods', :id => @good.id,
        :user_id => @good.seller_id, :subject => @good.name, :body => uzenet} %></p>

<p><%= link_to 'Vissza', {:action => 'index', :controller => 'goods'} %></p>
