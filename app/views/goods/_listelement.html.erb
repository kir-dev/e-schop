
<div class="uk-text-center" width="500px" height="500px" >
        <div class="uk-inline-clip uk-transition-toggle" tabindex="0">
            <% if listelement.photo.attached? %>
              <%= image_tag listelement.photo,height: "80px", width: "30px"%>
            <% else %>
              <% product = Product.find_by_id(listelement.product_id) %>
              <% if product.photo.attached? %>
                <%= image_tag product.photo,width:"100%"%>
              <% end %> 
            <% end %>
                        
            <div class="uk-transition-fade uk-position-cover  uk-overlay uk-overlay-default uk-flex uk-flex-center uk-flex-middle">
              <%if controller_name=="products"%>
              <%= link_to "Kiválaszt", product_new_from_product_path( :good => { :product_id =>listelement.id }),:method=> :post,class: "uk-h1 uk-margin-remove" %> 
              <%else%>
                <%= link_to listelement.name, url_for(listelement), class: "uk-h1 uk-margin-remove" %>
                <%end%>
            </div>
        </div>
</div>