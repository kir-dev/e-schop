<% @prev_tags_arr.each do |t_arr| %>
    <div >
        <% t_arr.each do |t| %>
            <span class="uk-badge">
                <%= t %>
                <%= link_to goods_index_get_path(deleted_tag: t, prev_tags: @prev_tags_str, floors: @selected_floors), method: :get do %> <span class = "uk-text-secondary" uk-icon="icon: close" uk-tooltip="title: Törlés; pos: bottom"></span><%end%>
            </span>
        <% end %>
    </div>
<% end %>